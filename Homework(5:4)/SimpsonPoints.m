function I = FirstDerivUnique(x,y)
% Author: Shiv Toolsidass

numIntervals = length(x)-1;

%If number of intervals in the data points is divisible by 3,do integration
%by composite 
if(mod(numIntervals,3) == 0)
    
    
end
end
